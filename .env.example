# Kraken API credentials
NITRO_KRAKEN_API_KEY=
NITRO_KRAKEN_PRIVATE_KEY=

# API authentication (optional, leave empty to disable)
NITRO_API_TOKEN=

# Grid trading parameters
NITRO_GRID_LOWER_PRICE=80000
NITRO_GRID_UPPER_PRICE=100000
NITRO_GRID_LEVELS=10
NITRO_ORDER_SIZE_USDC=50

# Sandbox mode (true = validate orders on Kraken but don't execute)
NITRO_SANDBOX_MODE=true

# Post-only orders (true = maker fees only, rejects taker orders)
NITRO_POST_ONLY=false

# Grid recentering (true = auto-recenter grid when price drifts)
NITRO_GRID_RECENTERING=false

# Dynamic spacing via ATR volatility (optional, disabled by default)
NITRO_VOLATILITY_ENABLED=false
NITRO_ATR_PERIOD=14
NITRO_SPACING_MIN_MULTIPLIER=0.5
NITRO_SPACING_MAX_MULTIPLIER=2.0

# Stop-Loss / Take-Profit (0 = disabled)
NITRO_STOP_LOSS_PERCENT=0
NITRO_TAKE_PROFIT_USDC=0

# Sentry error tracking (optional, leave empty to disable)
NITRO_SENTRY_DSN=
